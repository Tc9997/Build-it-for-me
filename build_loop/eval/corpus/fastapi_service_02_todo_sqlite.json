{
  "id": "fastapi_service_02",
  "name": "Todo API with SQLite",
  "archetype": "fastapi_service",
  "idea": "A REST API for a todo list backed by SQLite. Endpoints: POST /todos (title, description, optional due_date), GET /todos with ?completed=true/false filter, PATCH /todos/{id} to update fields, DELETE /todos/{id}. Include /health endpoint. Use SQLite via sqlite3 standard library, no ORM.",
  "expected_signals": [
    {
      "type": "file_exists",
      "description": "App exists",
      "file_path": "src/app.py"
    },
    {
      "type": "file_exists",
      "description": "Routes exist",
      "file_path": "src/routes.py"
    },
    {
      "type": "import_check",
      "description": "sqlite3 available",
      "module_name": "sqlite3"
    }
  ],
  "difficulty": "medium",
  "tags": ["rest", "crud", "database"]
}
